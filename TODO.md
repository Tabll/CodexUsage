# TODO

CodexPlus 当前已经完成从关键决策到可分发包的第一轮闭环。后续 TODO 不再按早期开发阶段无限展开，而是按“已完成基线”和“下一步路线”维护。

## 已完成基线

| 阶段 | 状态 | 结果 |
| --- | --- | --- |
| 第 0 阶段：关键决策 | 已完成 | 确认 Codex 桌面端为第一版数据源，最低支持 macOS 26，面向分发。 |
| 第 1 阶段：项目脚手架 | 已完成 | 创建 SwiftUI macOS 菜单栏 App，隐藏 Dock 图标，提供基础弹窗和退出操作。 |
| 第 2 阶段：用量模型 | 已完成 | 建立 `UsageSnapshot`、`UsageProvider`、`UsageService` 和 Mock provider。 |
| 第 3 阶段：UI MVP | 已完成 | 状态栏、菜单栏弹窗、刷新、显示模式和数据源选择完成。 |
| 第 4 阶段：真实用量数据源 | 已完成 | 读取 Codex 桌面端 SQLite 日志，解析 usage 和 `codex.rate_limits`。 |
| 第 5 阶段：预算与提醒 | 已完成 | 支持每日预算、警告阈值、预算状态和可选 macOS 通知。 |
| 第 6 阶段：持久化 | 已完成 | 持久化设置、数据源配置和最后一次有效用量快照。 |
| 第 7 阶段：测试 | 已完成 | 覆盖模型、解析器、服务状态、菜单栏弹窗和设置窗口冒烟测试。 |
| 第 8 阶段：打包发布 | 已完成 | App 图标、版本号、Release 脚本、Developer ID 签名、公证说明和分发包验证完成。 |

## 当前体验基线

- [x] 默认状态栏显示 `5小时 x% 本周 x%`。
- [x] 菜单栏弹窗保持轻量，只放核心用量概览和操作。
- [x] 预算、提醒、数据源和状态栏显示配置放入独立设置窗口。
- [x] 用量刷新防重入，文件变更刷新带防抖。
- [x] 真实数据源查询避免对日志正文做大范围 `LIKE` 扫描。
- [x] Developer ID 构建时自动加入安全时间戳。
- [x] 已验证 notarization、stapler 和 `syspolicy_check distribution`。
- [x] 已推送到 GitHub：`git@github.com:Tabll/CodexUsage.git`。

## 下一步：产品体验

- [ ] 设置窗口增加“关于 CodexPlus”区域，显示版本号、构建号和 GitHub 链接。
- [ ] 菜单栏弹窗增加“复制诊断信息”操作，便于排查数据源问题。
- [ ] 支持自定义状态栏文字模板，例如 `5h {short}% · week {weekly}%`。
- [ ] 支持隐藏某些弹窗指标，让弹窗更贴合个人工作流。
- [ ] 支持快捷键打开设置窗口或刷新数据。

## 下一步：数据与统计

- [ ] 按项目、工作区或会话聚合用量。
- [ ] 增加最近 7 天 / 30 天历史图表。
- [ ] 增加每日用量明细列表。
- [ ] 支持导出 CSV 或 JSON。
- [ ] 明确 Codex 会员、非会员、月度额度金额等不同计划的展示策略。
- [ ] 为 Codex 日志格式变化增加更强的兼容测试。

## 下一步：稳定性

- [ ] 增加真实 SQLite fixture，覆盖数据库读取层。
- [ ] 增加文件监听防抖和刷新防重入的单元测试。
- [ ] 增加数据源不可用时的恢复测试。
- [ ] 优化大日志库下的查询性能和解析上限策略。
- [ ] 增加 App 启动性能基准。

## 下一步：发布与分发

- [ ] 将 notarization、stapler 和最终 zip 重新打包整合进 `scripts/package-release.sh` 的可选参数。
- [ ] 增加 `CHANGELOG.md`。
- [ ] 明确版本发布流程和版本号递增规则。
- [ ] 评估 Sparkle 或其他自动更新机制。
- [ ] 准备首个 GitHub Release 的说明文案和安装截图。

## 暂不做

- [ ] 默认请求 Codex/OpenAI 网络接口获取用量。
- [ ] 在本地保存 API Key。
- [ ] 在未明确授权时上传日志、路径或用量数据。
- [ ] 支持 macOS 26 以下系统。
